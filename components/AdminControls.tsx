'use client';
import { useState } from 'react'; import { createSupabaseBrowserClient } from '@/lib/supabase/client';
export function AdminControls(){const db=createSupabaseBrowserClient();const [url,setUrl]=useState('');const [out,setOut]=useState('');const [busy,setBusy]=useState(false);
 async function act(action:string,extra:any={}){setBusy(true);setOut('');const {data:{session}}=await db.auth.getSession();const res=await fetch(`${process.env.NEXT_PUBLIC_SUPABASE_URL}/functions/v1/admin-sync`,{method:'POST',headers:{'content-type':'application/json','authorization':`Bearer ${session?.access_token}`},body:JSON.stringify({action,...extra})});setOut(JSON.stringify(await res.json(),null,2));setBusy(false);if(res.ok&&action!=='inspect')setTimeout(()=>location.reload(),600)}
 return <div className="adminControls"><div className="rowActions"><button className="primaryBtn" disabled={busy} onClick={()=>act('scan')}>Trigger source scan</button><button className="ghostBtn" disabled={busy} onClick={()=>act('bootstrap')}>Initial catalog import</button></div><div className="adminUrl"><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="Authorized source anime or episode URL"/><button className="ghostBtn" disabled={busy||!url} onClick={()=>act('sync_url',{url})}>Queue URL</button><button className="ghostBtn" disabled={busy||!url} onClick={()=>act('inspect',{url})}>Inspect parser</button></div>{out&&<pre className="adminOutput">{out}</pre>}</div>}
export function RetryButton({id}:{id:string}){const db=createSupabaseBrowserClient();const [busy,setBusy]=useState(false);async function retry(){setBusy(true);const {data:{session}}=await db.auth.getSession();await fetch(`${process.env.NEXT_PUBLIC_SUPABASE_URL}/functions/v1/admin-sync`,{method:'POST',headers:{'content-type':'application/json','authorization':`Bearer ${session?.access_token}`},body:JSON.stringify({action:'retry',id})});location.reload()}return <button className="tinyBtn" disabled={busy} onClick={retry}>{busy?'…':'Retry'}</button>}

export function DisableSourceButton({id,disabled}:{id:string;disabled?:boolean}){const db=createSupabaseBrowserClient();const [busy,setBusy]=useState(false);async function disable(){setBusy(true);const {data:{session}}=await db.auth.getSession();await fetch(`${process.env.NEXT_PUBLIC_SUPABASE_URL}/functions/v1/admin-sync`,{method:'POST',headers:{'content-type':'application/json','authorization':`Bearer ${session?.access_token}`},body:JSON.stringify({action:'disable_source',id})});location.reload()}return <button className="tinyBtn" disabled={busy||disabled} onClick={disable}>{disabled?'Disabled':busy?'…':'Disable'}</button>}
