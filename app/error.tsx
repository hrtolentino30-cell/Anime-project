'use client';export default function ErrorPage({reset}:{error:Error&{digest?:string};reset:()=>void}){return <div className="pageWidth standalone narrow"><div className="emptyState"><span className="eyebrow">SOMETHING WENT WRONG</span><h1>We couldn’t load this view.</h1><p>The sync pipeline runs independently; retrying this page is safe.</p><button className="primaryBtn" onClick={reset}>Try again</button></div></div>}
