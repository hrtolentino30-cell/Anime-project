import { redirect } from 'next/navigation';import { createSupabaseServerClient } from '@/lib/supabase/server';import { AnimeCard } from '@/components/AnimeCard';export const dynamic='force-dynamic';export default async function MyList(){const db=await createSupabaseServerClient();const {data:{user}}=await db.auth.getUser();if(!user)redirect('/login');const {data}=await db.from('favorites').select('created_at,anime:anime_id(*)').eq('user_id',user.id).order('created_at',{ascending:false});return <div className="pageWidth standalone"><div className="pageIntro"><span className="eyebrow">SAVED FOR YOU</span><h1>My List</h1></div><div className="browseGrid">{data?.map((x:any)=>x.anime&&<AnimeCard key={x.anime.id} anime={x.anime}/>)}</div>{!data?.length&&<div className="emptyState"><h2>Your list is empty</h2><p>Add anime from any detail page.</p></div>}</div>}
